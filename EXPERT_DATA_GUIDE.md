# איך למלא את טבלת הדוגמאות (data/expert_labels.csv)

## מבנה הטבלה

| עמודה | הסבר | דוגמה |
|---|---|---|
| `face_shape` | צורת הפנים (באנגלית, קטן): `oval` / `round` / `square` / `heart` / `diamond` | `round` |
| `frame_shape` | צורת/סגנון המסגרת - **בדיוק** כמו שכתוב בשדה `shape_tag` ב-`data/catalog.json` | `מרובעות` |
| `verdict` | פסק הדין: `good` (טוב) / `bad` (לא מתאים) / `neutral` (ניטרלי) / `excellent` (מצוין - משקל כפול) | `good` |
| `notes` | הסבר קצר חופשי (אופציונלי, יופיע בהסבר למשתמש) | "מרכך את קו הלסת" |

## חשוב
- כל שורה = דוגמה אחת שאת "מלמדת" את המערכת
- **ככל שיש יותר דוגמאות לאותו צירוף (צורת פנים + צורת מסגרת), הציון מדויק יותר** - אם תכתבי גם `round + מרובעות + good` וגם עוד שורה דומה, זה יחזק את הציון
- אפשר וכדאי לכתוב גם דוגמאות `bad` (מה **לא** להמליץ) - זה חשוב לא פחות מה-`good`
- `frame_shape` חייב להיות תואם בדיוק לכתיב שמופיע בקטלוג (`data/catalog.json`, שדה `shape_tag`), אחרת המסגרות לא "ייתפסו" נכון

## איך מריצים אחרי כל עדכון

```
.venv\Scripts\python.exe src\learn_rules.py
```

זה קורא את `data/expert_labels.csv` ובונה מחדש את `data/learned_rules.json`.
**חובה להריץ את זה כל פעם שמעדכנים את הטבלה**, אחרת השינויים לא יכנסו לתוקף באפליקציה.

## קובץ לדוגמה
יש כבר תבנית עם 12 דוגמאות ב-`data/expert_labels_template.csv` - אפשר להעתיק אותו
ל-`data/expert_labels.csv` ולהמשיך למלא אותו, או להתחיל טבלה חדשה משלך.
